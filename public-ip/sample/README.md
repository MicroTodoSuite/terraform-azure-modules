# public-ip sample

Creates one Standard static address named `lex-mts-fprd-pip-sample` with local
state. Fill the subscription and an existing resource group in
`terraform.tfvars` first:

```bash
terraform init
terraform plan
```
