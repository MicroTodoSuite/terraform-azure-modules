# identity sample

Creates one user-assigned managed identity named `lex-mts-fprd-id-sample`
with local state. Fill the subscription and an existing resource group in
`terraform.tfvars` first:

```bash
terraform init
terraform plan
```
