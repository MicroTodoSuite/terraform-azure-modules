# network sample

Creates one VNet and one private node subnet with local state. Fill the
subscription and an existing resource group in `terraform.tfvars` first:

```bash
terraform init
terraform plan
```
